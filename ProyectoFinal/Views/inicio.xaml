<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProyectoFinal.Views.inicio"
             Title="inicio">
    <VerticalStackLayout Padding="20" Spacing="20">

        <Label Text="Bienvenido al Sistema de Diagnóstico" 
                   FontSize="24"
                   FontAttributes="Bold"
                   HorizontalOptions="Center" />

        <Image Source="logo.png" HeightRequest="400" HorizontalOptions="Center" />
        <Grid RowDefinitions="Auto, Auto, Auto" ColumnDefinitions="*, 2*, Auto">
            <Button Text="Redes Sociales"
                Clicked="Button_Clicked"
                BackgroundColor="Purple"
                TextColor="White"
                Grid.Row="0"
                Grid.Column="2"
                FontSize="14"
                VerticalOptions="Start"/>
        </Grid>
        <Button Text="Empezar"
                    Clicked="OnEmpezarClicked"
                    BackgroundColor="LightBlue" />

    </VerticalStackLayout>
</ContentPage>